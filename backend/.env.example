# FastAPI Application Settings
DEBUG=false
LOG_LEVEL=INFO
HOST=0.0.0.0
PORT=4000

# Directory Settings (relative to project root)
TMP_DIR=tmp

# Database Settings
DB_NAME=ai_slop_extension
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# Google Cloud Storage Settings (required for media storage)
GCS_BUCKET_NAME=your-bucket-name  # GCS bucket name for media storage (REQUIRED)
GCS_PROJECT_ID=  # GCP project ID (auto-detect if empty)
GCS_CREDENTIALS_PATH=  # Path to service account key file (optional, uses ADC if empty)

# Google Gemini Settings
GEMINI_API_KEY=your-gemini-api-key
