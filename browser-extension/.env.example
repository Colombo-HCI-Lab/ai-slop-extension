# Chrome Web Store API Credentials
# Get these from Google Cloud Console after enabling Chrome Web Store API

# Your extension ID from Chrome Web Store (visible in the URL after first upload)
CHROME_EXTENSION_ID=your_extension_id_here

# OAuth 2.0 Client credentials from Google Cloud Console
CHROME_CLIENT_ID=your_client_id_here.apps.googleusercontent.com
CHROME_CLIENT_SECRET=your_client_secret_here

# Setup Instructions:
# 1. Go to Google Cloud Console (https://console.cloud.google.com/)
# 2. Enable Chrome Web Store API
# 3. Create OAuth 2.0 credentials (Web application type)
# 4. Upload your extension to Chrome Web Store manually first to get the extension ID
# 5. Copy the values above and remove the comments
# 6. Run: npm run publish
